# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.env

# Artifacts (training outputs, regenerable)
artifacts/
!artifacts/.gitkeep
# Precomputed model cache, shipped so a fresh deploy does not refit for ~90s.
# Valid because files_fingerprint hashes content, not mtime.
!artifacts/base_predictor.pkl
!artifacts/base_predictor.pkl.fp

# External source caches (regenerable / best-effort)
footy/data/external/cache/
!footy/data/external/cache/.gitkeep

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
.claude/

# Test / tooling
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# Local secrets
configs/secrets.local.yaml
